---
title: Security
description: Content Security Policy, threat model, and how to report a vulnerability responsibly.
icon: Shield
---

import { Callout } from "fumadocs-ui/components/callout";

SabbathCue runs as a Tauri v2 desktop app, which means a Chromium-derived
WebView with elevated privileges (filesystem, IPC, network).
Compromising the WebView would compromise the host. We treat that
seriously.

## Content Security Policy

The Tauri WebView ships with a restrictive CSP defined verbatim in
`src-tauri/tauri.conf.json`. The full directive list:

```
default-src 'self';
script-src 'self';
style-src 'self' 'unsafe-inline';
img-src 'self' data: blob:;
font-src 'self' data:;
connect-src 'self';
media-src 'self' blob:;
worker-src 'self';
frame-src 'none';
frame-ancestors 'none';
object-src 'none';
base-uri 'self';
form-action 'self';
manifest-src 'self'
```

A few things worth calling out:

- **`script-src 'self'`** — no inline scripts, no `eval`, no
  third-party JS. The Tauri runtime injects its IPC bootstrap before
  the CSP applies.
- **`style-src 'self' 'unsafe-inline'`** — required by Radix /
  shadcn for runtime style injection on portals and animations.
- **`connect-src 'self'`** — the WebView itself only talks to the
  Tauri IPC channel. Outbound network traffic (Deepgram WebSocket,
  Bible data downloads) all happens from the Rust side, so the
  webview doesn't need to allow-list those endpoints.
- **`frame-ancestors 'none'`** — the WebView can't be embedded
  anywhere.
- **`object-src 'none'`** + **`base-uri 'self'`** — block plugin
  vectors and base-tag injection.

## Reporting a vulnerability

<Callout title="Please report privately first" type="warn">
If you find a security issue, **do not open a public GitHub issue.**
Send a private report by following the instructions in
[SECURITY.md](https://github.com/Bongisto/SabbathCue/blob/main/.github/SECURITY.md)
in the repo.
</Callout>

## Tauri capabilities

SabbathCue follows Tauri v2's principle-of-least-privilege capability
system. Capability files live under `src-tauri/capabilities/` —
today there are two: `default.json` and `desktop.json`.

If you're reviewing the codebase for security:

1. Start at `src-tauri/capabilities/*.json` to see which Tauri
   commands and plugins are reachable from each window.
2. Trace each granted command to its handler in `rhema-api`.
3. Audit the handler's input validation and side effects.

## Remote control defaults

![Settings → Remote tab with bind-host fields highlighted, port-bind error states, and the live command log](/docs/screenshots/settings-remote-binding.png)

*Settings → Remote panels for starting OSC and HTTP listeners. Port-bind failures show inline so you can spot a misconfigured port without leaving the dialog. **Click to expand.***

The OSC and HTTP listeners are bound to localhost only by default:

- **OSC**: bind host `127.0.0.1`, port `8000`.
- **HTTP**: bind host `127.0.0.1`, port `8080`, with bearer-token
  authentication required for all control endpoints.

The HTTP API requires a cryptographically random bearer token that
is generated on first launch and stored in the system keychain. Every
control request must include `Authorization: Bearer <token>`; requests
without a valid token receive `401 Unauthorized`.

Because both listeners bind to `127.0.0.1`, no external device can
reach them — the API is accessible only from the local machine. This
protects the operator from unauthorized remote control without
requiring firewall configuration.

LAN-wide reachability is not exposed in the current Settings UI. If a
future release adds it (e.g., for a Stream Deck on a separate device),
it should be an explicit opt-in with authentication, clear operator
warnings, and firewall guidance.

There is no built-in rate limit on either protocol — the
API is intentionally simple so it composes with whatever you
already trust.

## Defaults that protect the operator

- **No telemetry** — SabbathCue does not phone home. There's no
  analytics, no crash reporter, no usage tracking.
- **Local-first by default** — Vosk runs locally, the database
  lives on disk, and Deepgram is opt-in. No keys, no internet, no
  problem.
- **Bundled SQLite** — `rusqlite` with the `bundled` feature, so
  the binary doesn't link against a system SQLite that could be
  swapped out.
