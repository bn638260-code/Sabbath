---
title: Platform Setup
description: System packages and build tools required on macOS, Linux, and Windows.
icon: Settings2
---

import { Tab, Tabs } from "fumadocs-ui/components/tabs"
import { Callout } from "fumadocs-ui/components/callout"

SabbathCue is a Tauri v2 app with native Rust dependencies. Pick the block for
your operating system below.

## macOS

Run `xcode-select --install` if you have not installed the Xcode Command Line
Tools yet.

## Linux

<Tabs items={["Debian / Ubuntu", "Arch / Manjaro"]}>
  <Tab value="Debian / Ubuntu">

Follow the [Tauri v2 Linux prerequisites](https://v2.tauri.app/start/prerequisites/?utm_source=rhema-docs#linux)
for your distribution.

  </Tab>
  <Tab value="Arch / Manjaro">

Follow the [Tauri v2 Linux prerequisites](https://v2.tauri.app/start/prerequisites/?utm_source=rhema-docs#linux)
for your distribution.

  </Tab>
</Tabs>

You also need the [Tauri v2 Linux prerequisites](https://v2.tauri.app/start/prerequisites/?utm_source=rhema-docs#linux)
(WebKitGTK, libssl, etc.). Tauri's installer prints exact `apt`/`pacman`
commands for your distribution.

## Windows

Windows needs the MSVC C++ build tools before the shared setup pipeline.

<Callout title="Restart the terminal" type="warn">
  After installing new build tools, close the terminal and open a new one so
  PATH changes are inherited.
</Callout>

1. Install [Visual Studio 2022](https://visualstudio.microsoft.com/downloads/)
   with the **Desktop development with C++** workload (provides MSVC).
2. From the repo root in PowerShell:

   ```powershell
   bun install
   bun run setup:windows
   ```

   This checks for MSVC and prints install guidance if the toolchain is missing.

3. **Close the terminal and open a new one** if you installed new build tools.
4. Continue with the [installation guide](/docs/getting-started/installation):

   ```powershell
   bun run setup:all
   bun run tauri dev
   ```

## Verifying the toolchain

A quick way to confirm everything is wired before kicking off `setup:all`:

```bash
bun --version
rustc --version
python3 --version
```

If any command isn't found, jump back to [Prerequisites](/docs/getting-started/prerequisites)
and reinstall.

To import PowerPoint decks (sermon slides or theme backgrounds), also install
[LibreOffice](https://www.libreoffice.org/). SabbathCue locates `soffice` on
your `PATH` or the default install directory; set `SABBATHCUE_SOFFICE_PATH` to
override the location. Verify it with `soffice --version`.

A successful platform check prints a version string for each command in turn. If
any of them prints "command not found", install the missing tool before running
`setup:all`.
